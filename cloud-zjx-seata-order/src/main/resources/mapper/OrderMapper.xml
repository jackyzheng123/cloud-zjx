<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zjx.mapper.OrderMapper">

    <insert id="insert" parameterType="com.zjx.dto.OrderDTO">
        insert into order_tbl(user_id, order_no, commodity_code, `count`, money)
        values (#{userId}, #{orderNo}, #{commodityCode}, #{count}, #{money})
    </insert>

</mapper>